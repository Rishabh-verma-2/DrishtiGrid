# Dependencies (too large — install with npm install / pip install)
node_modules/
package-lock.json

# Python Virtual Environment (too large — recreate with: python -m venv .venv)
.venv/
venv/
env/

# Python Cache (auto-generated)
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/

# Environment Variables & Secrets (NEVER commit credentials)
.env
.env.local
.env.*.local

# Logs
*.log

# Build Outputs
dist/
build/
.cache/

# IDE & OS Files
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# Model Weights (too large — download via ai-service/download_models.py)
*.pt
*.pth
!ai-service/model_weights/.gitkeep
